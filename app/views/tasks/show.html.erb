<h1>show task</h1>

<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<%= @task.completed ? "[x] This task is completed" : "[ ] This task is not completed" %> </br>
</br>

<%= link_to "Update this task", edit_path %> </br>

<%= link_to "Delete", task_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %></br>


<%= link_to "Back to list", tasks_path %>
